#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий
	
Процедура ПередЗаписью(Отказ)
	
	// Вызывается непосредственно до записи объекта в базу данных
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	КлючНастройки = ОбменСКонтрагентамиСлужебный.НовыйКлючНастройкиОтправки();
	КлючНастройки.Отправитель = Организация;
	КлючНастройки.Получатель  = Контрагент;
	КлючНастройки.Договор     = ДоговорКонтрагента;
	Наименование = ОбменСКонтрагентамиСлужебный.ПредставлениеНастройкиОтправкиЭлектронныхДокументов(КлючНастройки);
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли