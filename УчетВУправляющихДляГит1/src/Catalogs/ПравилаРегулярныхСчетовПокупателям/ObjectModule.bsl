#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

Процедура ПриЗаписи(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	Если НЕ Выполняется Тогда
		Возврат;
	КонецЕсли;
	
	Шаблон = Неопределено;
	ДатаСледующего = Неопределено;
	
	Если НЕ ДополнительныеСвойства.Свойство("Шаблон", Шаблон)
		Или НЕ ДополнительныеСвойства.Свойство("ДатаСледующего", ДатаСледующего) Тогда
		Возврат;
	КонецЕсли;
	
	ОрганизацияШаблон = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Шаблон, "Организация");
	
	Если Организация <> ОрганизацияШаблон Тогда
		Организация = ОрганизацияШаблон;
	КонецЕсли;
	
	РегистрыСведений.РегулярныеСчетаПокупателям.УстановитьШаблон(Ссылка, Шаблон, ДатаСледующего);
	
КонецПроцедуры

#КонецЕсли
