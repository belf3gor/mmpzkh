
//////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ ФОРМЫ

#Область ОбработчикиФормы

&НаСервере
// Обрыботчик события "ПриСозданииНаСервере" формы.
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	// Установим список доступных видов работ.
	Если Параметры.Свойство("СписокДоступныхВидовРабот") Тогда
		УПЖКХ_ТиповыеМетодыКлиентСервер.УстановитьЭлементОтбора(Список.Отбор, "Ссылка", Параметры.СписокДоступныхВидовРабот, ВидСравненияКомпоновкиДанных.ВСписке);
	КонецЕсли;
	
	// Получим список Выбранных видов работ из параметров.
	Параметры.Свойство("СписокВыбранныхВидовРабот", СписокВыбранныхВидовРабот);
	
КонецПроцедуры

#КонецОбласти

//////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ ЭЛЕМЕНТОВ ФОРМЫ

#Область ОбработчикиЭлементовФормы

&НаКлиенте
// Обрыботчик события "Выбор" поля "Список".
//
Процедура СписокВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	Если ЗначениеЗаполнено(ВыбраннаяСтрока)
	   И СписокВыбранныхВидовРабот.НайтиПоЗначению(ВыбраннаяСтрока) = Неопределено Тогда
		
		// Добавим оборудование в список.
		СписокВыбранныхВидовРабот.Добавить(ВыбраннаяСтрока);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
// Обрыботчик события "ПередНачаломДобавления" поля "СписокВыбранныхВидовРабот".
//
Процедура СписокВыбранныхВидовРаботПередНачаломДобавления(Элемент, Отказ, Копирование, Родитель, Группа, Параметр)
	
	Отказ = Истина;
	
КонецПроцедуры

#КонецОбласти

//////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ КОМАНД ФОРМЫ

#Область ОбработчикиКомандФормы

&НаКлиенте
// Обработчик команды "ЗавершитьПодбор".
//
Процедура ЗавершитьПодбор(Команда)
	
	Закрыть(СписокВыбранныхВидовРабот);
	
КонецПроцедуры

#КонецОбласти