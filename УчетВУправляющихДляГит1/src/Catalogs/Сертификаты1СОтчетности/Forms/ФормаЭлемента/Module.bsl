

&НаКлиенте
Процедура ПолучитьСертификат(Команда)
	
	Адрес = ПолучитьСертификатНаСервере();
	Если ЗначениеЗаполнено(Адрес) Тогда
		ПолучитьФайл(Адрес, Объект.Идентификатор + ".cer", Истина);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция ПолучитьСертификатНаСервере()
	
	Сертификат = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Объект.Ссылка, "Сертификат").Получить();
	Если ЗначениеЗаполнено(Сертификат) Тогда
		Возврат ПоместитьВоВременноеХранилище(Сертификат);
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции


