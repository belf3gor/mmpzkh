
#Область ОбработчикиКоманды

&НаКлиенте
// Процедура обработчик команды.
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ОтборОбъект = ПараметрКоманды;
	
	ИсточникКоманды = ПараметрыВыполненияКоманды.Источник;
	
	// Если уоманда выполнена из формы выбора помещения, тогда возможно объектом будет не помещение а комната.
	Если ТипЗнч(ИсточникКоманды) = Тип("УправляемаяФорма")
	   И ИсточникКоманды.ИмяФормы = "Справочник.УПЖКХ_Помещения.Форма.ФормаВыбора"
	   И ИсточникКоманды.ТекущийЭлемент.Имя = "ПомещенияКвартиры" Тогда
		
		ОтборОбъект = ИсточникКоманды.Элементы.ПомещенияКвартиры.ТекущаяСтрока;
		
	КонецЕсли;
	
	ПараметрыФормы = Новый Структура("ОтборОбъект", ОтборОбъект);
	ОткрытьФорму("РегистрСведений.КВП_ПлощадьПомещения.ФормаСписка", ПараметрыФормы, ПараметрыВыполненияКоманды.Источник, ПараметрыВыполненияКоманды.Уникальность, ПараметрыВыполненияКоманды.Окно);
	
КонецПроцедуры

#КонецОбласти