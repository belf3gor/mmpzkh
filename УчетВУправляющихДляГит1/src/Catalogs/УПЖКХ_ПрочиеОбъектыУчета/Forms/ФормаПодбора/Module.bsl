#Область ОбработчикиСобытийФормы

&НаСервере
// Обрыботчик события "ПриСозданииНаСервере" формы.
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	// Установим список доступного оборудования.
	Если Параметры.Свойство("СписокДоступногоОборудования") Тогда
		УПЖКХ_ТиповыеМетодыКлиентСервер.УстановитьЭлементОтбора(Список.Отбор, "Ссылка", Параметры.СписокДоступногоОборудования, ВидСравненияКомпоновкиДанных.ВСписке);
	КонецЕсли;
	
	// Получим список выбранного оборудования из параметров.
	Параметры.Свойство("СписокВыбранногоОборудования", СписокВыбранногоОборудования);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовФормы

&НаКлиенте
// Обрыботчик события "Выбор" поля "Список".
//
Процедура СписокВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	Если ЗначениеЗаполнено(ВыбраннаяСтрока)
	   И СписокВыбранногоОборудования.НайтиПоЗначению(ВыбраннаяСтрока) = Неопределено Тогда
		
		// Добавим оборудование в список.
		СписокВыбранногоОборудования.Добавить(ВыбраннаяСтрока);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
// Обрыботчик события "ПередНачаломДобавления" поля "СписокВыбранногоОборудования".
//
Процедура СписокВыбранногоОборудованияПередНачаломДобавления(Элемент, Отказ, Копирование, Родитель, Группа, Параметр)
	
	Отказ = Истина;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
// Обработчик команды "ЗавершитьПодбор".
//
Процедура ЗавершитьПодбор(Команда)
	
	Закрыть(СписокВыбранногоОборудования);
	
КонецПроцедуры

#КонецОбласти