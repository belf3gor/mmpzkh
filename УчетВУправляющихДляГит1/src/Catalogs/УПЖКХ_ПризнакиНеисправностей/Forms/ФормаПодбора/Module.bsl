
#Область ОбработчикиСобытийФормы

&НаСервере
// Обрыботчик события "ПриСозданииНаСервере" формы.
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	// Установим список доступных признаков неисправности.
	Если Параметры.Свойство("СписокДоступныхПризнаковНеисправности") Тогда
		УПЖКХ_ТиповыеМетодыКлиентСервер.УстановитьЭлементОтбора(Список.Отбор, "Ссылка", Параметры.СписокДоступныхПризнаковНеисправности, ВидСравненияКомпоновкиДанных.ВСписке);
	КонецЕсли;
	
	// Получим список выбранных признаков неисправности из параметров.
	Параметры.Свойство("СписокВыбранныхПризнаковНеисправности", СписокВыбранныхПризнаковНеисправности);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовФормы

&НаКлиенте
// Обрыботчик события "Выбор" поля "Список".
//
Процедура СписокВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	Если ЗначениеЗаполнено(ВыбраннаяСтрока)
	   И СписокВыбранныхПризнаковНеисправности.НайтиПоЗначению(ВыбраннаяСтрока) = Неопределено Тогда
		
		// Добавим признак неисправности в список.
		СписокВыбранныхПризнаковНеисправности.Добавить(ВыбраннаяСтрока);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
// Обрыботчик события "ПередНачаломДобавления" поля "СписокВыбранныхПризнаковНеисправности".
//
Процедура СписокВыбранныхПризнаковНеисправностиПередНачаломДобавления(Элемент, Отказ, Копирование, Родитель, Группа, Параметр)
	
	Отказ = Истина;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
// Обработчик команды "ЗавершитьПодбор".
//
Процедура ЗавершитьПодбор(Команда)
	
	Закрыть(СписокВыбранныхПризнаковНеисправности);
	
КонецПроцедуры

#КонецОбласти