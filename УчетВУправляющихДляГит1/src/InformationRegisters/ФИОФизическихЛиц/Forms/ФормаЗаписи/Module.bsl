
#Область ОбработчикиСобытийФормы

&НаКлиенте
Процедура ПослеЗаписи(ПараметрыЗаписи)
	Оповестить("ИзменениеЛичныхДанных", Запись.ФизическоеЛицо);
КонецПроцедуры

// Квартплата +

&НаСервере
// Процедура - обработчик события "ПриСозданииНаСервере" формы.
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если ЗначениеЗаполнено(Параметры.ЗначениеКопирования) Тогда
		Запись.ДокументРегистратор = ПредопределенноеЗначение("Документ.КВП_ИзменениеПаспортныхДанных.ПустаяСсылка");
		Запись.ПричинаИзменения = "";
	КонецЕсли;
	ТолькоПросмотр = Не Запись.ДокументРегистратор.Пустая();
	
КонецПроцедуры

// Квартплата -

#КонецОбласти
