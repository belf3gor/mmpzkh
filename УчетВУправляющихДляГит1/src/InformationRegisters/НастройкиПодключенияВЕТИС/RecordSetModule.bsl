
#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)
	
	Если Количество() > 0 Тогда
		ОтборХозяйствующийСубъект = Отбор.Найти("ХозяйствующийСубъект");
		
		Если ОтборХозяйствующийСубъект <> Неопределено Тогда
			ХозяйствующийСубъект = ЭтотОбъект[0].ХозяйствующийСубъект;
			СоответствуетОрганизации = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ХозяйствующийСубъект, "СоответствуетОрганизации");
			
			Если СоответствуетОрганизации Тогда
				ПроверяемыеРеквизиты.Добавить("Администратор");
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли