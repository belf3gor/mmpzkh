
#Область ОбработчикиСобытийФормы

&НаКлиенте
// Обработчик события "ПередЗаписью" формы.
//
Процедура ПередЗаписью(Отказ, ПараметрыЗаписи)
	
	Если НЕ смсСервер.НомерТелефонаКорректен(СокрЛП(Запись.НомерТелефона))
	 ИЛИ НЕ ЗначениеЗаполнено(Запись.НомерТелефона) Тогда
		ТекстСообщения = "Номер телефона некорректен!" + Символы.ПС + "Номер телефона должен содержать 11 числовых символов.";
		смсОбщегоНазначения.СообщитьПользователю(ТекстСообщения,, "НомерТелефона", "Запись", Отказ);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти
