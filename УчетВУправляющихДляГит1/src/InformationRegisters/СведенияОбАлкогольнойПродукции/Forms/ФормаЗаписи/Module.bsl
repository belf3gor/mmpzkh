
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	РегистрыСведений.СведенияОбАлкогольнойПродукции.СписокВидовПродукции(Элементы.КодВида169.СписокВыбора);
		
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура КодВида169ПриИзменении(Элемент)
	                     
	ЭлементСписка = Элемент.СписокВыбора.НайтиПоЗначению(Запись.КодВида169);
	Если ЭлементСписка <> Неопределено Тогда
		Запись.НаименованиеВида169 = СтрЗаменить(ЭлементСписка.Представление, Запись.КодВида169 + " - ", "");
	Иначе
		Запись.НаименованиеВида169 = "";
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти
