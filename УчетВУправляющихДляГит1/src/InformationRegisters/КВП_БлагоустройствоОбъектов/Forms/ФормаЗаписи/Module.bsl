
////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ ФОРМЫ

#Область ОбработчикиСобытийФормы

&НаСервере
// Обработчик события формы "ПриСозданииНаСервере".
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("ОтборОбъект") Тогда
		Запись.Объект = Параметры.ОтборОбъект;
		Элементы.Объект.ТолькоПросмотр = Истина;
	КонецЕсли;
	
	Если Параметры.Свойство("ВидБлагоустройства") Тогда
		Запись.ВидБлагоустройства = Параметры.ВидБлагоустройства;
	КонецЕсли;
	
	Если Параметры.Свойство("Значение") Тогда
		Запись.Значение = Параметры.Значение;
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
// Обработчик события формы "ПослеЗаписи".
Процедура ПослеЗаписи(ПараметрыЗаписи)
	
	Оповестить("ОбновитьТаблицуБлагоустройствоОбъектов");
	
КонецПроцедуры

#КонецОбласти