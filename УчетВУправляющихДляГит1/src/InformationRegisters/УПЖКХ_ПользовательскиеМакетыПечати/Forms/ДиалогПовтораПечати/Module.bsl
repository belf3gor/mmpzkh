
////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ ЭЛЕМЕНТОВ ФОРМЫ

#Область ОбработчикиСобытийЭлементовФормы

// Процедура - обработчик команды "ИзменитьРабочийКаталогПечати".
//
&НаКлиенте
Процедура ИзменитьРабочийКаталогПечати(Команда)
	
	Результат = "";
			//ОткрытьФормуМодально(
			//			"РегистрСведений.УПЖКХ_ПользовательскиеМакетыПечати.Форма.НастройкаКаталогаФайловПечати");
	
	Если ТипЗнч(Результат) = Тип("Строка") Тогда
		КаталогДляСохраненияДанныхПечати = Результат;
	КонецЕсли;
	
КонецПроцедуры

// Процедура - обработчик команды "ПовторитьПечать".
//
&НаКлиенте
Процедура ПовторитьПечать(Команда)
	Закрыть(КаталогДляСохраненияДанныхПечати);
КонецПроцедуры

// Процедура - обработчик событя "При Создании " , при условии создания на стороне сервера.
//
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	КаталогДляСохраненияДанныхПечати = УПЖКХ_ТиповыеМетодыСервер.ПолучитьЛокальныйКаталогФайловПечати();
	Элементы.Сообщение.Заголовок = Элементы.Сообщение.Заголовок + Символы.ПС + Параметры.СообщениеОбОшибке;
	
КонецПроцедуры

#КонецОбласти