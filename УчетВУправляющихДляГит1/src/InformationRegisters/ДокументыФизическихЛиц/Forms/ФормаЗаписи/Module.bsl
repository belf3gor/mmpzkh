
#Область ОбработчикиСобытийФормы

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ВидДокументаПриИзменении(Элемент)
	
	Если ЯвляетсяУдостоверениемЛичности(Запись.Физлицо, Запись.ВидДокумента, Запись.Период) Тогда
		Запись.ЯвляетсяДокументомУдостоверяющимЛичность = Истина;
	КонецЕсли;
	
	СотрудникиКлиентСервер.ОбновитьГруппуФамилияИмяЛатиницей(ЭтотОбъект, "Запись");
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервереБезКонтекста
Функция ЯвляетсяУдостоверениемЛичности(Физлицо, ВидДокумента, Дата)
	
	Возврат РегистрыСведений.ДокументыФизическихЛиц.ЯвляетсяУдостоверениемЛичности(Физлицо, ВидДокумента, Дата);
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Процедура УстановитьВидимостьДоступность(Форма)
	
	СотрудникиКлиентСервер.УстановитьВидимостьГруппыФамилияИмяЛатиницей(Форма, "Запись");
	
КонецПроцедуры

&НаКлиенте
Процедура ПередЗаписью(Отказ, ПараметрыЗаписи)
	
	СотрудникиКлиентСервер.ПроверитьНаписаниеФИДокументаЛатинскими(ЭтотОбъект, "Запись", Отказ);
	
КонецПроцедуры

#КонецОбласти

#Область ПроцедурыПодсистемыКвартплаты

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	// Квартплата +
	Если Не Параметры.ЗначениеКопирования.Пустой() Тогда
		Запись.ДокументРегистратор = ПредопределенноеЗначение("Документ.КВП_ИзменениеПаспортныхДанных.ПустаяСсылка");
		Запись.ПричинаИзменения = "";
	КонецЕсли;
	ТолькоПросмотр = ЗначениеЗаполнено(Запись.ДокументРегистратор);
	// Квартплата -
	
	УстановитьВидимостьДоступность(ЭтотОбъект);
	
КонецПроцедуры

// Квартплата +

&НаКлиенте
// Обработчик события "НачалоВыбора" поля ввода "ДокументКемВыдан".
//
Процедура КемВыданНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	ФормаВыбора = ПолучитьФорму("Справочник.КВП_ОВД.ФормаВыбора");
	ФормаВыбора.ВладелецФормы = ЭтаФорма;
	ФормаВыбора.ОписаниеОповещенияОЗакрытии = Новый ОписаниеОповещения("ОбработатьВыборОВД", ЭтаФорма);
	ФормаВыбора.Открыть();
	
КонецПроцедуры // КемВыданНачалоВыбора()

&НаКлиенте
// Асинхронный обработчик результата выбора элемента справочника "ОВД".
//
Процедура ОбработатьВыборОВД(ВыбранноеЗначение, ДополнительныеПараметры) Экспорт
	
	Если ВыбранноеЗначение <> Неопределено Тогда
		
		СтруктураРеквизитовОВД = УПЖКХ_ОбщегоНазначенияСервер.ПолучитьСтруктуруРеквизитовОВД(ВыбранноеЗначение);
		
		Запись.КемВыдан         = СтруктураРеквизитовОВД.Наименование;
		Запись.КодПодразделения = СтруктураРеквизитовОВД.КодПодразделения;
		
	КонецЕсли;
	
КонецПроцедуры // ОбработатьВыборОВД()

// Квартплата -

#КонецОбласти

