

////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ ФОРМЫ

#Область ОбработчикиСобытийФормы

&НаСервере
// Обработчик события формы "ПриСозданииНаСервере".
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("ОтборОбъект") Тогда
		Запись.Объект = Параметры.ОтборОбъект;
		Элементы.Объект.ТолькоПросмотр = Истина;
	КонецЕсли;
	
	Если Параметры.Свойство("Тип") Тогда
		Запись.Тип = Параметры.Тип;
	КонецЕсли;
		
	Если Параметры.Свойство("Площадь") Тогда
		 Запись.Площадь = Параметры.Площадь;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
// Обработчик события формы "ПослеЗаписи".
Процедура ПослеЗаписи(ПараметрыЗаписи)
	
	Оповестить("ОбновитьТаблицуПлощадей");
	
КонецПроцедуры

#КонецОбласти