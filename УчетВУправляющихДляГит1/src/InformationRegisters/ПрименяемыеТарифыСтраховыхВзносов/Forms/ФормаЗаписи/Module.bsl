
&НаКлиенте
Процедура ПериодПриИзменении(Элемент)
	
	Если НЕ ЗначениеЗаполнено(Запись.ДатаРегистрацииИзменений) Тогда
		Запись.ДатаРегистрацииИзменений = НачалоМесяца(Запись.Период);
	КонецЕсли;
	
КонецПроцедуры
