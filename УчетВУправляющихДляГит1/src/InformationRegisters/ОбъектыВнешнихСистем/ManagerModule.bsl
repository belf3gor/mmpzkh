#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Записывает в регистр набор объектов, записанных в сеансе.
// Не обязательно записывать все объекты за сеанс, можно записывать часть.
//
// Параметры:
//  ЗагруженныеОбъекты	 - Массив - ссылки на загруженные объекты
//  Сеанс				 - СправочникСсылка.СеансыЗагрузкиДанныхВнешнихСистем - сеанс, в ходе которого загружены (модифицированы) объекты
//
Процедура Зарегистрировать(ЗагруженныеОбъекты, Сеанс) Экспорт
	
	Для Каждого Объект Из ЗагруженныеОбъекты Цикл
		
		Запись = СоздатьМенеджерЗаписи();
		
		Запись.Объект = Объект;
		Запись.Сеанс  = Сеанс;
		
		Запись.Записать(Истина);
		
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли
