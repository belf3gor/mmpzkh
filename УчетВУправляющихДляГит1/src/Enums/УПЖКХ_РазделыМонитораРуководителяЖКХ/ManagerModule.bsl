#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область МетодыМенеджераПеречисления

// Возвращает массив разделов монитора руководителя по умолчанию
//  Возвращаемое значение:
//    Массив - Список разделов монитора руководителя доступных по умолчанию.
//
Функция РазделыМонитораРуководителяПоУмолчанию()
	
	РазделыПоУмолчанию = Новый Массив;
	РазделыПоУмолчанию.Добавить(СводныеДанныеПоНачислениямИОплатам);
	РазделыПоУмолчанию.Добавить(ДинамикаЗадолженностиПоВсемУслугам);
	
	Возврат РазделыПоУмолчанию;
	
КонецФункции

// Возвращает настройку списка разделов монитора руководителя ЖКХ по умолчанию
//  Возвращаемое значение:
//    СписокЗначений - Список разделов монитора руководителя с пометками и упорядочиванием по умолчанию.
//
Функция СписокРазделовМонитораРуководителяПоУмолчанию() Экспорт
	
	РазделыПоУмолчанию = РазделыМонитораРуководителяПоУмолчанию();
	
	СписокРазделов = Новый СписокЗначений();
	Для Каждого Раздел из Перечисления.УПЖКХ_РазделыМонитораРуководителяЖКХ Цикл
		
		Пометка = (РазделыПоУмолчанию.Найти(Раздел) <> Неопределено);
		СписокРазделов.Добавить(Раздел, Раздел, Пометка);
		
	КонецЦикла;
	
	Возврат СписокРазделов;
	
КонецФункции

// Возвращает раздел монитора, всегда выводимый в мониторе.
//
Функция ПолучитьОсновнойРазделМонитора() Экспорт
	
	Возврат СводныеДанныеПоНачислениямИОплатам;
	
КонецФункции

#КонецОбласти

#КонецЕсли