
#Область ПрограммныйИнтерфейс

// Функция формирует и возвращает список значений перечисления.
// 
// Возвращаемое значение:
//  СписокЗначений - список значений перечисления.
//
Функция ПолучитьСписокЗначений() Экспорт
	
	СписокВыбора = Новый СписокЗначений;
	
	Для Каждого ТекущееЗначение Из Перечисления.УПЖКХ_ТипНачисления Цикл
		СписокВыбора.Добавить(ТекущееЗначение, ТекущееЗначение);
	КонецЦикла;
	
	// В слуразделение начисления не требуется.
	СписокВыбора.Добавить(Перечисления.УПЖКХ_ТипНачисления.ПустаяСсылка(), "Без разделения");
	
	Возврат СписокВыбора;
	
КонецФункции

#КонецОбласти

#Область ОбработчикиСобытий

// Обработчик события "ОбработкаПолученияДанныхВыбора".
//
Процедура ОбработкаПолученияДанныхВыбора(ДанныеВыбора, Параметры, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	ДанныеВыбора = ПолучитьСписокЗначений();
	
КонецПроцедуры

#КонецОбласти
