Функция ЗначениеПоУмолчанию(ПредставлениеПериода) Экспорт

	Если СтрНайти(НРег(ПредставлениеПериода), "срокгодности") > 0 Тогда
		Возврат ПредопределенноеЗначение("Перечисление.ТочностьЗаполненияПериодаВЕТИС.Неприменимо");
	Иначе
		Возврат ПредопределенноеЗначение("Перечисление.ТочностьЗаполненияПериодаВЕТИС.Неизвестно");
	КонецЕсли; 

КонецФункции // ЗначениеПоУмолчанию()
 