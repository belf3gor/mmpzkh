
#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область СлужебныйПрограммныйИнтерфейс

// Возвращает массив значений перечисления, соответствующих авансовому счету-фактуре.
// Возвращаемое значение
//    ВидыСчетовФактурНаАванс - Массив.
Функция СчетаФактурыНаАванс() Экспорт
	
	ВидыСчетовФактурНаАванс = Новый Массив();
	ВидыСчетовФактурНаАванс.Добавить(Перечисления.ВидСчетаФактурыВыставленного.НаАванс);
	ВидыСчетовФактурНаАванс.Добавить(Перечисления.ВидСчетаФактурыВыставленного.НаАвансКомитента);
	ВидыСчетовФактурНаАванс.Добавить(Перечисления.ВидСчетаФактурыВыставленного.НаАвансКомитентаНаЗакупку);
	ВидыСчетовФактурНаАванс.Добавить(Перечисления.ВидСчетаФактурыВыставленного.КорректировочныйНаАванс);
	
	Возврат ВидыСчетовФактурНаАванс;
	
КонецФункции

#КонецОбласти

#КонецЕсли