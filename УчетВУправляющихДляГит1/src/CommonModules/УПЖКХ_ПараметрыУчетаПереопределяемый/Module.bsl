
////////////////////////////////////////////////////////////////////////////////
// ПЕРЕОПРЕДЕЛЯЕМЫЕ ФУНКЦИИ ПАРАМЕТРОВ УЧЕТА

#Область СлужебныеПроцедурыИФункции

// Функция возвращает признак, следует ли полностью распределять объем ОДН на лицевой счет,
// в случае, если расход по ИПУ по модулю меньше, чем отрицательный объем распределения ОДН.
Функция ВсегдаРаспределятьОтрицательныйОбъемОДНПолностью() Экспорт
	
	Возврат Ложь;
	
КонецФункции // ВсегдаРаспределятьОтрицательныйОбъемОДНПолностью()

// Функция возвращает признак, определяющий, нужно ли учитывать периоды действия услуги
// на лицевом счете при определении долей распределения ОДН в случае, когда распределение
// производится пропорционально прощади помещения.
// Функция по умолчанию возвращает значение Истина, т.е. периоды действия услуги
// на лицевом счете учитываются.
// При необходимости возвращаемое значение функции можно переопределить.
Функция УчитыватьПериодДействияУслугиПриРаспределенииОДНПоПлощади() Экспорт
	
	Возврат Истина;
	
КонецФункции // УчитыватьПериодДействияУслугиПриРаспределенииОДНПоПлощади()

#КонецОбласти