

#Область ПроцедурыОбработкиСообщений

// Обрабатывает сообщения об ошибках.
//
Процедура ВывестиСообщения(ТаблицаСообщений) Экспорт
	
	
	Для каждого ТекСтрока из ТаблицаСообщений Цикл
		
		ТекстСообщения = ТекСтрока.ТекстСообщения;
		ВидСообщения    = ТекСтрока.ВидСообщения;
		
		Если ТипЗнч(ТекстСообщения) = Тип("Строка") Тогда
			ВывестиСообщение(ТекстСообщения, ВидСообщения);
		КонецЕсли;
	КонецЦикла;
	
	ТаблицаСообщений.Очистить();
	
КонецПроцедуры

// Выводит диалоговое окно предупреждения с описанием ошибки.
//
Процедура ВывестиСообщение(ТекстСообщения, ВидСообщения)
	
	ПоказатьПредупреждение(, ТекстСообщения);
	
КонецПроцедуры

#КонецОбласти