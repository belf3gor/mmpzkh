#Область ПроцедурыУправленияНаСервере

// Размещает/изменяет кнопки на форме.
//
// Параметры:
//  Форма          - УправляемаяФорма - форма, на которой необходимо разместить/изменить кнопки.
//  ГруппаКнопки   - ЭлементФормы - группа, на которую(-ой) необходимо поместить/изменить кнопки.
//
Процедура ПриСозданииНаСервере(Форма, ГруппаКнопки = Неопределено) Экспорт
	
	// Проверка доступности пользователю стандартных подсистем.
	Если Не ПравоДоступа("Просмотр", Метаданные.Подсистемы.ОТР_СтандартныеПодсистемы) Тогда
		Возврат;
	КонецЕсли;
	
	// Схемы учета.
	ОТР_СхемыУчетаСервер.ПриСозданииНаСервере(Форма, ГруппаКнопки);
	
	// Часто задаваемые вопросы.
	ОТР_ЧастоЗадаваемыеВопросыСервер.ПриСозданииНаСервере(Форма, ГруппаКнопки);
	
	// Отображает текст кнопки "Справка".
	КнопкаСправка = Форма.Элементы.Найти("МакетыПечатныхФормСправка");
	Если КнопкаСправка = Неопределено Тогда 
		КнопкаСправка = Форма.Элементы.Найти("ФормаСправка");
	КонецЕсли;
	Если КнопкаСправка <> Неопределено Тогда
		КнопкаСправка.Отображение = ОтображениеКнопки.КартинкаИТекст;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти