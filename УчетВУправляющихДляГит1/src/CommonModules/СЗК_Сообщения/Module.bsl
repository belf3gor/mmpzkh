// Содержит в себе сообщения, формируемые в модулях конфигурации.
// Необходим для возможности перевода сообщений на другой язык.
//
// Изменения в модуле разрешены.

#Область ФункцииОграничения

// Сообщение "превышено допустимое количество объектов в " + ДопустимоеКоличество + " шт!".
//
// Параметры:
//  ДопустимоеКоличество - Число - допустимое количество объектов.
//
// Возвращаемое значение:
//  Строка.
//
Функция ПревышеноКоличествоОбъектов(ДопустимоеКоличество) Экспорт
	
	Возврат НСтр("ru = 'превышено допустимое количество объектов в " + ДопустимоеКоличество + " шт!'");
	
КонецФункции // ПревышеноКоличествоОбъектов()

// Сообщение "превышено допустимое количество строк в табличной части " + 
// ИмяТЧ + " в " + ДопустимоеКоличество + " шт!".
//
// Параметры:
//  ИмяТЧ        - Строка - имя табличной части
//  ДопустимоеКоличество - Число - допустимое количество строк ТЧ.
//
// Возвращаемое значение:
//  Строка.
//
Функция ПревышеноДопустимоеКоличествоСтрокТЧ(ИмяТЧ, ДопустимоеКоличество) Экспорт
	
	Возврат НСтр("ru = 'превышено допустимое количество строк в табличной части " + ИмяТЧ + " в " + ДопустимоеКоличество + " шт!'");
	
КонецФункции // ПревышеноДопустимоеКоличествоСтрокТЧ()

// Заголовок сообщений "Запись справочника " + Наименование.
//
// Параметры:
//  Наименование - Строка - наименование документа.
//
// Возвращаемое значение:
//  Строка.
//
Функция ЗаписьСправочника(Наименование) Экспорт
	
	Возврат НСтр("ru = 'Запись справочника " + Наименование + ":'");
	
КонецФункции // ЗаписьСправочника()

// Заголовок сообщений "Запись документа " + Наименование.
//
// Параметры:
//  Наименование - Строка - наименование документа.
//
// Возвращаемое значение:
//  Строка.
//
Функция ЗаписьДокумента(Наименование) Экспорт
	
	Возврат НСтр("ru = 'Запись документа " + Наименование + ":'");
	
КонецФункции // ЗаписьДокумента()

#КонецОбласти

#Область ОбщиеФункции

// Формирует текст сообщения об ошибке.
// Входные параметры:
// 1) ИтоговыйТекстСообщенияОбОшибке - формируемый текст ошибки для пользователя;
// 2) НовыйТекстОшибки       - текст текущей ошибки.
//
Процедура ДополнитьСообщенияОбОшибке(ИтоговыйТекстСообщенияОбОшибке, НовыйТекстОшибки) Экспорт
	
	// Получаем структуру с описанием ошибки и рекомандацией её устранения.
	ОписаниеОшибкиИРекомендацииДляУстранения = СЗК_ТиповыеМетоды.ПолучитьОписаниеИРекомендацииДляУстраненияПоПолномуТекстуОшибки(НовыйТекстОшибки);
	
	// Дополняем описание ошибки к исходному тексту ошибки.
	Если ЗначениеЗаполнено(ОписаниеОшибкиИРекомендацииДляУстранения) Тогда
		ОписаниеОшибкиИРекомендацииДляУстранения = ОписаниеОшибкиИРекомендацииДляУстранения + ":" + Символы.ПС + НовыйТекстОшибки;
	Иначе
		ОписаниеОшибкиИРекомендацииДляУстранения = НовыйТекстОшибки;
	КонецЕсли;
	
	ИтоговыйТекстСообщенияОбОшибке = ИтоговыйТекстСообщенияОбОшибке
								   + ?(Не ПустаяСтрока(ИтоговыйТекстСообщенияОбОшибке), Символы.ПС, "")
								   + ОписаниеОшибкиИРекомендацииДляУстранения;
	
КонецПроцедуры

// Сообщение "Ошибка при подключении внешней компоненты СЛК".
//
// Возвращаемое значение:
//  Строка
//
Функция ОшибкаПодключенияКомпонентыСЛК() Экспорт
	
	Возврат НСтр("ru = 'Ошибка подключения внешней компоненты СЛК
					   |Возможно подключение внешних компонент заблокировано на уровне ОС (в клиент-серверной ИБ подключение компоненты производится на сервере 1С)'");
	
КонецФункции // ОшибкаПодключенияКомпонентыСЛК()

// Сообщение "Ошибка создания менеджера объектов СЛК".
//
// Возвращаемое значение:
//  Строка
//
Функция ОшибкаСозданияМенеджераОбъектовСЛК() Экспорт
	
	Возврат НСтр("ru = 'Ошибка создания менеджера объектов СЛК'");
	
КонецФункции // ОшибкаСозданияМенеджераОбъектовСЛК()

// Сообщение "Ошибка проверки номера версии СЛК!".
//
// Возвращаемое значение:
//  Строка
//
Функция ОшибкаПроверкиНомераВерсииСЛК(ТекущаяВерсия = "", АктуальнаяВерсия = "") Экспорт
	
	ОписаниеТекущихВерсий = "";
	
	Если Не ПустаяСтрока(ТекущаяВерсия) Или Не ПустаяСтрока(АктуальнаяВерсия) Тогда
		ОписаниеТекущихВерсий = Символы.ПС + "Текущая версия - " + ТекущаяВерсия
							  + Символы.ПС + "Минимально допустимая версия - " + АктуальнаяВерсия;
	КонецЕсли;
	
	Возврат НСтр("ru = 'Ошибка проверки номера версии клиентской компоненты СЛК" + ОписаниеТекущихВерсий + "
					   |Обновите версию клиентской компоненты СЛК (в клиент-серверной ИБ установка компоненты производится на сервере 1С)'");
	
КонецФункции // ОшибкаПроверкиНомераВерсииСЛК()

// Сообщение "Превышено допустимое число сеансов!".
//
// Возвращаемое значение:
//  Строка
//
Функция ОшибкаПревышенияЧислаСеансов() Экспорт
	
	Возврат НСтр("ru = 'Превышено максимально допустимое количество пользовательских сеансов Базовой версии программы'");
	
КонецФункции // ОшибкаПревышенияЧислаСеансов()

#КонецОбласти
