#Область СлужебныеПроцедурыИФункции

// Формирует наименование подъезда по введенным реквизитам.
//
// Параметры:
//  Нет
//
Функция ОбновитьНаименованиеПодъезда(Объект, мАктуаленСтроительныйАдрес) Экспорт
	
	//ОбновитьАктуальностьСтроительногоАдреса(Объект);
	Если мАктуаленСтроительныйАдрес Тогда
		Наименование = "Секция № " + Объект.Секция;
	Иначе
		Наименование = "Подъезд №" + Объект.НомерПодъезда + "" + Объект.Суффикс;
	КонецЕсли;
	
	Возврат Наименование;
	
КонецФункции // ОбновитьНаименованиеПодъезда()

// Вызывает заполнение переменной мАктуаленСтроительныйАдрес, если она не инициализирована.
//
// Параметры:
//  ПроверятьЗаполненность - Булево - нужно ли проверять заполненность переменной.
//
Функция ОбновитьАктуальностьСтроительногоАдреса(Здание, ПроверятьЗаполненность = Истина) Экспорт
	
	Если НЕ ПроверятьЗаполненность Тогда
		Возврат УПЖКХ_РаботаСОбъектамиУчетаСервер.УстановитьАктуальностьСтроительногоАдреса(Здание);//, ЭтоНовый());
	КонецЕсли;
	
КонецФункции

#КонецОбласти