#Область СлужебныеПроцедурыИФункции

&НаКлиенте
// Обработчик команды.
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ИмяСхемыУчета = "УПЖКХ_СхемаУчетаЛьготыИСкидки";
	
	// В параметрах передадим имя макета, из которого должна быть загружена схема учета.
	ПараметрыФормы = Новый Структура("ИмяМакета", ИмяСхемыУчета);
	
	// Откроем форму со схемой учета подсистемы "Льготы и скидки".
	ОткрытьФорму("ОбщаяФорма.ОТР_СхемыУчета", ПараметрыФормы, , ИмяСхемыУчета);
	
КонецПроцедуры

#КонецОбласти