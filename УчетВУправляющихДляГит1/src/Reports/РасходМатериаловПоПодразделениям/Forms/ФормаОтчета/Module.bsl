
&НаСервере
Процедура СформироватьНаСервере()
	     	//{{КОНСТРУКТОР_ЗАПРОСА_С_ОБРАБОТКОЙ_РЕЗУЛЬТАТА
	// Данный фрагмент построен конструктором.
	// При повторном использовании конструктора, внесенные вручную изменения будут утеряны!!!
	
	Макет = Отчеты.РасходМатериаловПоПодразделениям.ПолучитьМакет("Макет");
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	РасходМатериалов.Период,
		|	РасходМатериалов.Регистратор,
		|	ПРЕДСТАВЛЕНИЕ(РасходМатериалов.Регистратор),
		|	РасходМатериалов.НомерСтроки,
		|	РасходМатериалов.Активность,
		|	РасходМатериалов.Склад,
		|	ПРЕДСТАВЛЕНИЕ(РасходМатериалов.Склад),
		|	РасходМатериалов.Подразделение,
		|	ПРЕДСТАВЛЕНИЕ(РасходМатериалов.Подразделение),
		|	РасходМатериалов.Здание,
		|	ПРЕДСТАВЛЕНИЕ(РасходМатериалов.Здание),
		|	РасходМатериалов.КатегорияВыполняемыхРабот,
		|	ПРЕДСТАВЛЕНИЕ(РасходМатериалов.КатегорияВыполняемыхРабот),
		|	РасходМатериалов.ВидРабот,
		|	ПРЕДСТАВЛЕНИЕ(РасходМатериалов.ВидРабот),
		|	РасходМатериалов.Номенклатура,
		|	ПРЕДСТАВЛЕНИЕ(РасходМатериалов.Номенклатура),
		|	РасходМатериалов.Цена,
		|	РасходМатериалов.Сумма,
		|	РасходМатериалов.Количество,
		|	РасходМатериалов.ЕдиницаИзмерения,
		|	ПРЕДСТАВЛЕНИЕ(РасходМатериалов.ЕдиницаИзмерения),
		|	РасходМатериалов.ОтветственныйЗаВыполнение,
		|	ПРЕДСТАВЛЕНИЕ(РасходМатериалов.ОтветственныйЗаВыполнение),
		|	РасходМатериалов.СтатусВыполнения,
		|	РасходМатериалов.ФлагВыполнения,
		|	РасходМатериалов.МестоПроведенияРабот,
		|	РасходМатериалов.Ответственный,
		|	ПРЕДСТАВЛЕНИЕ(РасходМатериалов.Ответственный)
		|ИЗ
		|	РегистрСведений.РасходМатериалов КАК РасходМатериалов
		|ГДЕ
		|	РасходМатериалов.Период МЕЖДУ &Период1 И &Период2
		|	И РасходМатериалов.Подразделение = &Подразделение";
	
	Запрос.УстановитьПараметр("Период1", период.ДатаНачала);
	Запрос.УстановитьПараметр("Период2", период.ДатаОкончания);
	Запрос.УстановитьПараметр("Подразделение", Подразделение);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ОбластьЗаголовок = Макет.ПолучитьОбласть("Заголовок");
	ОбластьПодвал = Макет.ПолучитьОбласть("Подвал");
	ОбластьШапкаТаблицы = Макет.ПолучитьОбласть("ШапкаТаблицы");
	ОбластьПодвалТаблицы = Макет.ПолучитьОбласть("ПодвалТаблицы");
	ОбластьДетальныхЗаписей = Макет.ПолучитьОбласть("Детали");
	
	ТабДок.Очистить();
	ТабДок.Вывести(ОбластьЗаголовок);
	ТабДок.Вывести(ОбластьШапкаТаблицы);
	ТабДок.НачатьАвтогруппировкуСтрок();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		ОбластьДетальныхЗаписей.Параметры.Заполнить(ВыборкаДетальныеЗаписи);
		ТабДок.Вывести(ОбластьДетальныхЗаписей, ВыборкаДетальныеЗаписи.Уровень());
	КонецЦикла;
	
	ТабДок.ЗакончитьАвтогруппировкуСтрок();
	ТабДок.Вывести(ОбластьПодвалТаблицы);
	ТабДок.Вывести(ОбластьПодвал);
	
	//}}КОНСТРУКТОР_ЗАПРОСА_С_ОБРАБОТКОЙ_РЕЗУЛЬТАТА

КонецПроцедуры

&НаКлиенте
Процедура Сформировать(Команда)
	СформироватьНаСервере();
КонецПроцедуры
