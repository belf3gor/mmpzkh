
#Область ОбработчикКоманды

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ПараметрыВызова = Новый Структура("Источник, Окно");
	ЗаполнитьЗначенияСвойств(ПараметрыВызова, ПараметрыВыполненияКоманды);
	
	ФормаПараметры = Новый Структура;
	ФормаПараметры.Вставить("Заголовок", НСТР("ru = 'Список форм регистрационного учета'"));
	
	ФормаВладелец     = ПараметрыВыполненияКоманды.Источник;
	ФормаУникальность = ПараметрыВыполненияКоманды.Уникальность;
	ФормаОкно         = ПараметрыВыполненияКоманды.Окно;
	
	ОткрытьФорму("Отчет.КВП_ФормыРегистрационногоУчета.Форма.ФормаСписка", ФормаПараметры, ФормаВладелец, ФормаУникальность, ФормаОкно);
	
КонецПроцедуры

#КонецОбласти