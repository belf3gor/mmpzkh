
////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ ФОРМЫ

#Область Обработчики_событий_формы

&НаСервере
// Обработчик события "ПриСозданииНаСервере" формы.
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("СписокТиповДиаграмм") Тогда
		СписокТиповДиаграмм = Параметры.СписокТиповДиаграмм;
	Иначе
		СписокТиповДиаграмм = Отчеты.УПЖКХ_СводныеДанныеПоНачислениямИОплатам.ПолучитьСписокТиповДиаграмм();
	КонецЕсли;
	
	Для Каждого ТекущийТип Из СписокТиповДиаграмм Цикл
		Элементы.ТипДиаграммыПоУмолчанию.СписокВыбора.Добавить(ТекущийТип.Значение, ТекущийТип.Представление);
	КонецЦикла;
	
	Если Параметры.Свойство("ТипДиаграммыПоУмолчанию") Тогда
		ТипДиаграммыПоУмолчанию = Параметры.ТипДиаграммыПоУмолчанию;
	Иначе
		ТипДиаграммыПоУмолчанию = Элементы.ТипДиаграммыПоУмолчанию.СписокВыбора[0];
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ КОМАНД ФОРМЫ

#Область Обработчики_событий_команд_формы

&НаКлиенте
// Обработчик команды "СохранитьИЗакрыть" формы.
Процедура СохранитьИЗакрыть(Команда)
	
	Закрыть(Новый Структура("ТипДиаграммыПоУмолчанию", ТипДиаграммыПоУмолчанию));
	
КонецПроцедуры

&НаКлиенте
// Обработчик команды "Отмена" формы.
Процедура Отмена(Команда)
	
	Закрыть();
	
КонецПроцедуры

#КонецОбласти

////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ ЭЛЕМЕНТОВ ФОРМЫ

#Область Обработчики_событий_элементов_формы

&НаКлиенте
// Обработчик события "ПриИзменении" поля "ТипДиаграммыПоУмолчанию".
Процедура ТипДиаграммыПоУмолчаниюПриИзменении(Элемент)
	
	Модифицированность = Истина;
	
КонецПроцедуры

&НаКлиенте
// Обработчик события "Очистка" поля "ТипДиаграммыПоУмолчанию".
Процедура ТипДиаграммыПоУмолчаниюОчистка(Элемент, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
КонецПроцедуры

#КонецОбласти

