
#Область ОбработчикиКомандФормы

&НаКлиенте
// Процедура обработчик команды "Отправить".
//
Процедура КомандаОтправить(Команда)
	
	СтруктураРассылки = Новый Структура;
	СтруктураРассылки.Вставить("ЗаголовокСообщения",    ЗаголовокСообщения);
	СтруктураРассылки.Вставить("ТекстСообщения",        ТекстСообщения);
	СтруктураРассылки.Вставить("ДатаНачалаРассылки",    ДатаНачалаРассылки);
	СтруктураРассылки.Вставить("ДатаОкончанияРассылки", ДатаОкончанияРассылки);
	
	УПЖКХ_ИнтеграцияСМобильнымПриложениемВзаимодействиеССервером.ОтправитьРассылкуПушУведомлений(СтруктураРассылки);
	
КонецПроцедуры

#КонецОбласти