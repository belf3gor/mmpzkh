
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.ПечатьСписка Тогда
		
		Заголовок = НСтр("ru = 'Сверка налогов с ФНС'");
		ДанныеДляПечати.Вывести(
			Обработки.СверкаНалоговСФНС.ПечатьСпискаОплат(Параметры.АдресХранилища, Параметры.ТолькоПроблемные));
			
	Иначе // расшифровка состояния платежа
		Заголовок = НСтр("ru = 'Состояние платежа'");
		ДанныеДляПечати.Вывести(Обработки.СверкаНалоговСФНС.ПечатьРасшифровкиСостоянияОплатыНалога(Параметры.ДанныеОплаты));
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовФормы

&НаКлиенте
Процедура РасшифровкаСостоянияВыбор(Элемент, Область, СтандартнаяОбработка)
	
	Если Область.Имя = "АдресФНС" Тогда
		СтандартнаяОбработка = Ложь;
		УправлениеКонтактнойИнформациейКлиентБП.ПоказатьНаКартеНажатие(ЭтотОбъект, Элемент, Область.Текст);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти
