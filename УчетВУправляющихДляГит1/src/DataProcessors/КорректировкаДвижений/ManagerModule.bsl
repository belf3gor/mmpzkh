#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

Функция ИнформацияОПроведенииДокумента(ДокументДвижений) Экспорт
	
	МенедежерыДокументов = ДокументыСИнформациейОПроведении();
	МенедежерДокумента = МенедежерыДокументов.Получить(ТипЗнч(ДокументДвижений));
	Если МенедежерДокумента <> Неопределено Тогда
		Возврат МенедежерДокумента.ИнформацияОПроведенииДокумента(ДокументДвижений);
	КонецЕсли;
	
	Возврат "";
	
КонецФункции

Функция ДокументыСИнформациейОПроведении()
	
	ТипыДокументов = Новый Соответствие();
	ТипыДокументов.Вставить(Тип("ДокументСсылка.СписаниеСРасчетногоСчета"), Документы.СписаниеСРасчетногоСчета);
	ТипыДокументов.Вставить(Тип("ДокументСсылка.ПоступлениеНаРасчетныйСчет"), Документы.ПоступлениеНаРасчетныйСчет);
	Возврат ТипыДокументов;
	
КонецФункции

#КонецЕсли
