
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	СформироватьОписаниеРасчета();
	
КонецПроцедуры

&НаСервере
Процедура СформироватьОписаниеРасчета()

	ОсновнойШрифт = Новый Шрифт(, 12);
	ЖирныйШрифт = Новый Шрифт(ОсновнойШрифт,,, Истина); 
	
	НовыйПараграф = ОписаниеРасчета.Элементы.Добавить();
	
	Текст = НСтр("ru='Размер суточных устанавливается организацией – в коллективном договоре или локальном нормативном акте.'");
	НовыйЭлемент = НовыйПараграф.Элементы.Добавить(Текст, Тип("ТекстФорматированногоДокумента"));
	НовыйЭлемент.Шрифт = ОсновнойШрифт;
	
	НовыйЭлемент = НовыйПараграф.Элементы.Добавить(, Тип("ПереводСтрокиФорматированногоДокумента"));
	
	Текст = НСтр("ru='Допускается устанавливать разный размер для разных стран или регионов.'");
	НовыйЭлемент = НовыйПараграф.Элементы.Добавить(Текст, Тип("ТекстФорматированногоДокумента"));
	НовыйЭлемент.Шрифт = ОсновнойШрифт;
	
	Отступ = ОписаниеРасчета.Элементы.Добавить();
	
	НовыйПараграф = ОписаниеРасчета.Элементы.Добавить();
	
	Текст = НСтр("ru='Суточные могут облагаться НДФЛ и страховыми взносами, если их размер превышает нормы, установленные Налоговым кодексом.'");
	НовыйЭлемент = НовыйПараграф.Элементы.Добавить(Текст, Тип("ТекстФорматированногоДокумента"));
	НовыйЭлемент.Шрифт = ОсновнойШрифт;
	
	Отступ = ОписаниеРасчета.Элементы.Добавить();
	
	НовыйПараграф = ОписаниеРасчета.Элементы.Добавить();
	
	Текст = НСтр("ru='При командировках '");
	НовыйЭлемент = НовыйПараграф.Элементы.Добавить(Текст, Тип("ТекстФорматированногоДокумента"));
	НовыйЭлемент.Шрифт = ОсновнойШрифт;
	
	Текст = НСтр("ru='по России '");
	НовыйЭлемент = НовыйПараграф.Элементы.Добавить(Текст, Тип("ТекстФорматированногоДокумента"));
	НовыйЭлемент.Шрифт = ЖирныйШрифт;
	
	Текст = НСтр("ru='такая норма - '");
	НовыйЭлемент = НовыйПараграф.Элементы.Добавить(Текст, Тип("ТекстФорматированногоДокумента"));
	НовыйЭлемент.Шрифт = ОсновнойШрифт;
	
	Текст = НСтр("ru='%1 руб. '");
	Текст = СтрШаблон(Текст, Формат(УчетКомандировокКлиентСервер.НалоговаяНормаСуточныхПоРФ(), "ЧДЦ=0"));
	НовыйЭлемент = НовыйПараграф.Элементы.Добавить(Текст, Тип("ТекстФорматированногоДокумента"));
	НовыйЭлемент.Шрифт = ЖирныйШрифт;
	
	Текст = НСтр("ru='за каждый день; суточные не выплачиваются, если командировка начинается и заканчивается в один день.'");
	НовыйЭлемент = НовыйПараграф.Элементы.Добавить(Текст, Тип("ТекстФорматированногоДокумента"));
	НовыйЭлемент.Шрифт = ОсновнойШрифт;
	
	Отступ = ОписаниеРасчета.Элементы.Добавить();
	
	НовыйПараграф = ОписаниеРасчета.Элементы.Добавить();
	
	Текст = НСтр("ru='В командировках '");
	НовыйЭлемент = НовыйПараграф.Элементы.Добавить(Текст, Тип("ТекстФорматированногоДокумента"));
	НовыйЭлемент.Шрифт = ОсновнойШрифт;
	
	Текст = НСтр("ru='за границу '");
	НовыйЭлемент = НовыйПараграф.Элементы.Добавить(Текст, Тип("ТекстФорматированногоДокумента"));
	НовыйЭлемент.Шрифт = ЖирныйШрифт;
	
	Текст = НСтр("ru='России:'");
	НовыйЭлемент = НовыйПараграф.Элементы.Добавить(Текст, Тип("ТекстФорматированногоДокумента"));
	НовыйЭлемент.Шрифт = ОсновнойШрифт;
	
	Отступ = ОписаниеРасчета.Элементы.Добавить();
	
	НовыйПараграф = ОписаниеРасчета.Элементы.Добавить();
	НовыйПараграф.ТипПараграфа = ТипПараграфа.МаркированныйСписок;
	
	Текст = НСтр("ru='норма, установленная НК РФ - '");
	НовыйЭлемент = НовыйПараграф.Элементы.Добавить(Текст, Тип("ТекстФорматированногоДокумента"));
	НовыйЭлемент.Шрифт = ОсновнойШрифт;
	
	Текст = НСтр("ru='%1 руб. '");
	Текст = СтрШаблон(Текст, Формат(УчетКомандировокКлиентСервер.НалоговаяНормаСуточныхЗаРубежом(), "ЧДЦ=0"));
	НовыйЭлемент = НовыйПараграф.Элементы.Добавить(Текст, Тип("ТекстФорматированногоДокумента"));
	НовыйЭлемент.Шрифт = ЖирныйШрифт;

	Текст = НСтр("ru='за каждый день'");
	НовыйЭлемент = НовыйПараграф.Элементы.Добавить(Текст, Тип("ТекстФорматированногоДокумента"));
	НовыйЭлемент.Шрифт = ОсновнойШрифт;
	
	НовыйПараграф = ОписаниеРасчета.Элементы.Добавить();
	НовыйПараграф.ТипПараграфа = ТипПараграфа.МаркированныйСписок;
	
	Текст = НСтр("ru='если командировка начинается и заканчивается в один день, суточные выплачиваются в половинном размере'");
	НовыйЭлемент = НовыйПараграф.Элементы.Добавить(Текст, Тип("ТекстФорматированногоДокумента"));
	НовыйЭлемент.Шрифт = ОсновнойШрифт;
	
	НовыйПараграф = ОписаниеРасчета.Элементы.Добавить();
	НовыйПараграф.ТипПараграфа = ТипПараграфа.МаркированныйСписок;
	
	Текст = НСтр("ru='день выезда с территории России оплачивается по нормам загранкомандировки'");
	НовыйЭлемент = НовыйПараграф.Элементы.Добавить(Текст, Тип("ТекстФорматированногоДокумента"));
	НовыйЭлемент.Шрифт = ОсновнойШрифт;
	
	НовыйПараграф = ОписаниеРасчета.Элементы.Добавить();
	НовыйПараграф.ТипПараграфа = ТипПараграфа.МаркированныйСписок;
	
	Текст = НСтр("ru='день возвращения на территорию России оплачивается по нормам командировок по России'");
	НовыйЭлемент = НовыйПараграф.Элементы.Добавить(Текст, Тип("ТекстФорматированногоДокумента"));
	НовыйЭлемент.Шрифт = ОсновнойШрифт;
	
	НовыйПараграф = ОписаниеРасчета.Элементы.Добавить();
	НовыйПараграф.ТипПараграфа = ТипПараграфа.МаркированныйСписок;
	
	Текст = НСтр("ru='при следовании из одного иностранного государства в другое день пересечения границы оплачивается по нормам командировки в страну, в которую следует работник'");
	НовыйЭлемент = НовыйПараграф.Элементы.Добавить(Текст, Тип("ТекстФорматированногоДокумента"));
	НовыйЭлемент.Шрифт = ОсновнойШрифт;
	
	НовыйПараграф = ОписаниеРасчета.Элементы.Добавить();
	НовыйПараграф.ТипПараграфа = ТипПараграфа.МаркированныйСписок;
	
	Текст = НСтр("ru='даты пересечения границы определяются по отметкам в паспорте'");
	НовыйЭлемент = НовыйПараграф.Элементы.Добавить(Текст, Тип("ТекстФорматированногоДокумента"));
	НовыйЭлемент.Шрифт = ОсновнойШрифт;
	
	НовыйПараграф = ОписаниеРасчета.Элементы.Добавить();
	НовыйПараграф.ТипПараграфа = ТипПараграфа.МаркированныйСписок;
	
	Текст = НСтр("ru='если между странами заключены соглашения, по которым отметка о пересечения границы не ставится в паспорте, день пересечения границы определяется по билетам'");
	НовыйЭлемент = НовыйПараграф.Элементы.Добавить(Текст, Тип("ТекстФорматированногоДокумента"));
	НовыйЭлемент.Шрифт = ОсновнойШрифт;

	// Выравнивание по ширине
	Для каждого Параграф Из ОписаниеРасчета.Элементы Цикл
		Параграф.ГоризонтальноеПоложение = ГоризонтальноеПоложение.ПоШирине;
	КонецЦикла; 
	
КонецПроцедуры 
