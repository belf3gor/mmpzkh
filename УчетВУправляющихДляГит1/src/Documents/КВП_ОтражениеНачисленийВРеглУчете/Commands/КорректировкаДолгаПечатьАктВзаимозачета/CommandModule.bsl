
#Область СлужебныеПроцедурыИФункции

&НаСервере
// Возвращает массив документов.
Функция ПолучитьМассивДокументов(ПараметрКоманды)
	
	Возврат Документы.КВП_ОтражениеНачисленийВРеглУчете.ПолучитьМассивДокументовКорректировкаДолга(ПараметрКоманды);
	
КонецФункции

&НаКлиенте
// Обработчик команды.
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	МассивДокументов = ПолучитьМассивДокументов(ПараметрКоманды);
	
	Если МассивДокументов.Количество() > 0 Тогда
		
		УПЖКХ_ТиповыеМетодыКлиент.ВыполнитьКомандуПечати("Документ.КорректировкаДолга", "КорректировкаДолга", МассивДокументов,
			ПараметрыВыполненияКоманды, УПЖКХ_ТиповыеМетодыКлиент.ПолучитьЗаголовокПечатнойФормы(МассивДокументов));
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти