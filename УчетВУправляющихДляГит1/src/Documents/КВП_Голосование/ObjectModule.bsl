
#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытийДокумента

// Заполнение реквизитов значениями по умолчанию.
//
Процедура ОбработкаЗаполнения(ДанныеЗаполнения, СтандартнаяОбработка)
	
	УПЖКХ_ТиповыеМетодыСервер.Заполнить(ЭтотОбъект, ДанныеЗаполнения);
	
	Если ТипЗнч(ДанныеЗаполнения) = Тип("ДокументСсылка.КВП_РеестрУчастниковГолосования") Тогда
		Организация      = ДанныеЗаполнения.Организация;
		РеестрУчастников = ДанныеЗаполнения.Ссылка;
		
		Для Каждого ТекСтрокаУчастники Из ДанныеЗаполнения.Участники Цикл
			НоваяСтрока = РезультатыГолосования.Добавить();
			НоваяСтрока.ВесГолоса            = ТекСтрокаУчастники.ВесГолоса;
			НоваяСтрока.Помещение            = ТекСтрокаУчастники.Помещение;
			НоваяСтрока.СобственникПомещения = ТекСтрокаУчастники.СобственникПомещения;
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти 

#КонецЕсли