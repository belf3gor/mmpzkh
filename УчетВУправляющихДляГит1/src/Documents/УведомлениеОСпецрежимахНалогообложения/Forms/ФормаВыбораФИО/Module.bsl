#Область ОбработчикиСобытийФормы
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	// Пропускаем инициализацию, чтобы гарантировать получение формы при передаче параметра "АвтоТест".
	Если Параметры.Свойство("АвтоТест") Тогда
		Возврат;
	КонецЕсли;
	
	Параметры.Свойство("Фамилия", Фамилия);
	Параметры.Свойство("Имя", Имя);
	Параметры.Свойство("Отчество", Отчество);
	Параметры.Свойство("ИмяПоля", ИмяПоля);
	Параметры.Свойство("ИмяСтраницы", ИмяСтраницы);
КонецПроцедуры
#КонецОбласти

#Область ОбработчикиКомандФормы
&НаКлиенте
Процедура ОК(Команда)
	ДанныеОповещения = Новый Структура;
	ДанныеОповещения.Вставить("Фамилия", Фамилия);
	ДанныеОповещения.Вставить("Имя", Имя);
	ДанныеОповещения.Вставить("Отчество", Отчество);
	ДанныеОповещения.Вставить("Форма", ЭтотОбъект.ВладелецФормы);
	ДанныеОповещения.Вставить("ИмяПоля", ИмяПоля);
	ДанныеОповещения.Вставить("ИмяСтраницы", ИмяСтраницы);
	Закрыть(ДанныеОповещения);
КонецПроцедуры

&НаКлиенте
Процедура Отмена(Команда)
	Закрыть(КодВозвратаДиалога.Нет);
КонецПроцедуры

#КонецОбласти