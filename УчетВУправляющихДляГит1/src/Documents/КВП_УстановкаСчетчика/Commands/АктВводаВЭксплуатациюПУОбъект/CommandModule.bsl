
#Область ПроцедурыИФункцииОбработчикаКоманды

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	Если ПараметрыВыполненияКоманды.Источник.ИмяФормы = "Документ.КВП_УстановкаСчетчика.Форма.ФормаСписка" Тогда
		Возврат;
	КонецЕсли;
	ПараметрКоманды.Очистить();
	ПараметрКоманды.Добавить(ПараметрыВыполненияКоманды.Источник.Объект.Ссылка);
	
	ПараметрыПечати = Новый Структура;
	
	СтруктураЗаголовокФормы = УПЖКХ_ТиповыеМетодыКлиент.ПолучитьЗаголовокПечатнойФормы(ПараметрКоманды);
	Если НЕ СтруктураЗаголовокФормы = Неопределено Тогда
		ПараметрыПечати.Вставить("ЗаголовокФормы", СтруктураЗаголовокФормы.ЗаголовокФормы);
	КонецЕсли;
	
	ПараметрыПечати.Вставить("Объект", ПараметрыВыполненияКоманды.Источник.Элементы.Главная.ТекущиеДанные.Объект);
	
	УПЖКХ_ТиповыеМетодыКлиент.ВыполнитьКомандуПечати("Документ.КВП_УстановкаСчетчика", "АктВводаВЭксплуатациюПУ", ПараметрКоманды,
		ПараметрыВыполненияКоманды, ПараметрыПечати);
КонецПроцедуры

#КонецОбласти 
