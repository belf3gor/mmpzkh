
Процедура ОбработкаПроведения(Отказ, Режим)
	//{{__КОНСТРУКТОР_ДВИЖЕНИЙ_РЕГИСТРОВ
	// Данный фрагмент построен конструктором.
	// При повторном использовании конструктора, внесенные вручную изменения будут утеряны!!!

	// регистр РасходМатериаловТЭУ
	Движения.РасходМатериаловТЭУ.Записывать = Истина;
	Для Каждого ТекСтрокаНарядЗаказ Из НарядЗаказ Цикл
		Движение = Движения.РасходМатериаловТЭУ.Добавить();
		Движение.Период = Дата;
		Движение.АдресПроведенияРаботы = АдресПроведенияРаботы;
		Движение.ТеплоУзел2 = ТеплоУзел2;
		Движение.ТеплоУзел = ТеплоУзел;
		Движение.Подразделение = Подразделение;
		Движение.Номенклатура = ТекСтрокаНарядЗаказ.Номенклатура;
		Движение.Количество = ТекСтрокаНарядЗаказ.Количество;
		Движение.Цена = ТекСтрокаНарядЗаказ.Цена;
		Движение.Сумма = ТекСтрокаНарядЗаказ.Сумма;
		Движение.Заказчик = Заказчик;
		Движение.ДатаОкончанияРабот = ДатаОкончанияРабот;
		Движение.ДатаНачалоРабот = ДатаНачалоРабот;
		Движение.Склад = Склад;
		Движение.ЕдиницаИзмерения = ТекСтрокаНарядЗаказ.ЕдиницаИзмерения;
	КонецЦикла;

	//}}__КОНСТРУКТОР_ДВИЖЕНИЙ_РЕГИСТРОВ
КонецПроцедуры
