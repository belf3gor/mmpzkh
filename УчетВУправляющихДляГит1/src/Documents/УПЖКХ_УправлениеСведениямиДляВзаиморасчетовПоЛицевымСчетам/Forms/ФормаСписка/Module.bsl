
#Область ПрочиеПроцедурыИФункции

&НаКлиенте
// Процедура открывает форму предупреждения.
//
Процедура ПоказатьФормуПредупрежденияОСведенияхДляВзаиморасчетов()
	
	ОткрытьФорму("Документ.УПЖКХ_УправлениеСведениямиДляВзаиморасчетовПоЛицевымСчетам.Форма.ФормаПредупрежденияОСведенияхДляВзаиморасчетов",, ЭтаФорма);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийФормы

&НаСервере
// Обработчик события "ПриСозданииНаСервере" формы.
//
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	// Определим необходимость вывода предупреждения о сведениях для взаиморасчетов.
	ПоказыватьПредупреждения = Документы.УПЖКХ_УправлениеСведениямиДляВзаиморасчетовПоЛицевымСчетам.ПолучитьНастройкуВыводаПредупрежденияОСведенияхДляВзаиморасчетов();
	
	// ОбщиеМеханизмыИКоманды
	ОТР_ПодключаемыеОбщиеМеханизмыИКоманды.ПриСозданииНаСервере(ЭтаФорма);
	// Конец ОбщиеМеханизмыИКоманды
	
КонецПроцедуры

&НаКлиенте
// Обработчик события "ПриСозданииНаСервере" формы.
//
Процедура ПриОткрытии(Отказ)
	
	// Открываем форму предупреждения, если требуется.
	Если ПоказыватьПредупреждения Тогда
		ПодключитьОбработчикОжидания("ПоказатьФормуПредупрежденияОСведенияхДляВзаиморасчетов", 2, Истина);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

// ЧастоЗадаваемыеВопросы
&НаКлиенте
// Подключаемый обработчик команды перехода к часто задаваемым вопросам.
Процедура Подключаемый_ЧастоЗадаваемыеВопросыОткрытьСсылку(Команда)
	
	ОТР_ЧастоЗадаваемыеВопросыКлиент.Подключаемый_ЧастоЗадаваемыеВопросыОткрытьСсылку(Команда);
	
КонецПроцедуры
// Конец ЧастоЗадаваемыеВопросы

#КонецОбласти
